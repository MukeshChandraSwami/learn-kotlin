package com.learn

fun main(args: Array<String>) {

    var empSet: EmpSet = emptySet()

    val empMap: EmpMapIdAsKey = emptyMap()

    // StringBuilder is also a type alias of java.lang.StringBuilder
    val str: StringBuilder = StringBuilder("Hello")
    print(str)

}